[workspace]
members = ["crates/dxcluster-core", "src-tauri"]
resolver = "2"

[workspace.package]
version = "1.3.2"
edition = "2021"
rust-version = "1.77"
authors = ["dacr"]
license = "MIT"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = [
  "rt-multi-thread",
  "net",
  "io-util",
  "sync",
  "time",
  "macros",
  "process",
] }
tokio-util = { version = "0.7", features = ["codec"] }
bytes = "1"
futures-util = "0.3"
regex = "1"
once_cell = "1"
thiserror = "2"
anyhow = "1"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
rusqlite = { version = "0.32", features = ["bundled"] }
log = "0.4"
rumqttc = { version = "0.24", default-features = false }
socket2 = "0.6"
if-addrs = "0.13"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
